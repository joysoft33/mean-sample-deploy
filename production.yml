---

- name: Deploy {{ application_name }} node.js application
  hosts: all
  become: yes
  become_user: root
  remote_user: ubuntu
  vars:
    - setup_git_repo: yes
    - update_apt_cache: yes
  vars_files:
    - env_vars/local

  roles:
    - base
    - db
    - web
